from setuptools import setup, find_packages

setup(
    name='your_package_name',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[],
    author='Mario Antonio',
    author_email='m.antonio0294@gmail.com',
    description='A fun tool to create a canvas in the terminal.',
    url='https://github.com/therealmudd/python-canvas.git',
)
